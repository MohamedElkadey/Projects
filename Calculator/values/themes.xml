<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyApplication" parent="Base.Theme.MyApplication" />

    <style name="CalcButton" >
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:inset">0dp</item>
        <item name="backgroundTint">@color/button_background</item>
        <item name="android:textColor">@color/primary_text</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">22sp</item>
        <item name="android:foreground">?android:attr/selectableItemBackground</item>
    </style>

    <style name="AllClearButton" parent="CalcButton">
        <item name="android:textColor">@color/all_clear_text</item>
    </style>

    <style name="ActionButton" parent="CalcButton">
        <item name="android:textColor">@color/action_text</item>
    </style>

    <style name="EqualButton" parent="CalcButton">
        <item name="backgroundTint">@color/equal_background</item>
        <item name="android:textColor">@color/equal_button_text</item>

    </style>
    <style name="NumbersButton" parent="CalcButton">
        <item name="android:textColor">@color/black</item>
    </style>


</resources>